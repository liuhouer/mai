<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Mapper.ActivityFollow">
    <select id="getActivityFollowbyActivityID" parameterType="com.mai.activity.entity.ActivityFollow"
            resultType="com.mai.activity.entity.ActivityFollow">
        select * from ActivityFollow where  activityID=#{activityID}
    </select>
</mapper>